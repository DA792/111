<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scenic.mapper.intelligence.AIQARecordMapper">
    
    <!-- 结果映射 -->
    <resultMap id="AIQARecordResultMap" type="com.scenic.entity.intelligence.AIQARecord">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="question" property="question"/>
        <result column="answer" property="answer"/>
        <result column="knowledge_base_id" property="knowledgeBaseId"/>
        <result column="knowledge_graph_id" property="knowledgeGraphId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    
    <!-- 根据条件查询问答记录列表 -->
    <select id="selectByCondition" parameterType="map" resultMap="AIQARecordResultMap">
        SELECT * FROM ai_qa_record
        <where>
            <if test="userId != null">
                user_id = #{userId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>
    
</mapper>
