<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.scenic.mapper.user.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="UserResultMap" type="com.scenic.entity.user.User">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="real_name" property="realName" jdbcType="VARCHAR"/>
        <result column="id_type" property="idType" jdbcType="VARCHAR"/>
        <result column="id_number" property="idNumber" jdbcType="VARCHAR"/>
        <result column="phone" property="phone" jdbcType="VARCHAR"/>
        <result column="email" property="email" jdbcType="VARCHAR"/>
        <result column="avatar_file_id" property="avatarFileId" jdbcType="BIGINT"/>
        <result column="open_id" property="openId" jdbcType="VARCHAR"/>
        <result column="user_type" property="userType" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="INTEGER"/>
        <result column="register_time" property="registerTime" jdbcType="TIMESTAMP"/>
        <result column="last_login_time" property="lastLoginTime" jdbcType="TIMESTAMP"/>
        <result column="version" property="version" jdbcType="INTEGER"/>
        <result column="deleted" property="deleted" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 查询字段 -->
    <sql id="User_Column_List">
        id, user_name, password, real_name, id_type, id_number, phone, email, avatar_file_id, open_id, user_type, status, register_time, last_login_time, version, deleted, create_time, update_time
    </sql>

    <!-- 查询用户列表 -->
    <select id="selectList" resultMap="UserResultMap">
        SELECT 
        <include refid="User_Column_List"/>
        FROM user
        <where>
            <if test="username != null and username != ''">
                user_name LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND phone LIKE CONCAT('%', #{phone}, '%')
            </if>
        </where>
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 查询用户总数 -->
    <select id="selectCount" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM user
        <where>
            <if test="username != null and username != ''">
                user_name LIKE CONCAT('%', #{username}, '%')
            </if>
            <if test="phone != null and phone != ''">
                AND phone LIKE CONCAT('%', #{phone}, '%')
            </if>
        </where>
    </select>
    
    <!-- 小程序端查询用户列表 -->
    <select id="selectListForMiniapp" resultMap="UserResultMap">
        SELECT 
        <include refid="User_Column_List"/>
        FROM user
        <where>
            <if test="nickname != null and nickname != ''">
                real_name LIKE CONCAT('%', #{nickname}, '%')
            </if>
        </where>
        LIMIT #{offset}, #{limit}
    </select>
    
    <!-- 小程序端查询用户总数 -->
    <select id="selectCountForMiniapp" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM user
        <where>
            <if test="nickname != null and nickname != ''">
                real_name LIKE CONCAT('%', #{nickname}, '%')
            </if>
        </where>
    </select>
    
    <!-- 根据ID列表查询用户 -->
    <select id="selectByIds" resultMap="UserResultMap">
        SELECT 
        <include refid="User_Column_List"/>
        FROM user
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>