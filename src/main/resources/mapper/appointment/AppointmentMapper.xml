<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.scenic.mapper.appointment.AppointmentMapper">

    <!-- 结果映射 -->
    <resultMap id="AppointmentResultMap" type="com.scenic.entity.appointment.Appointment">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="user_id" property="userId" jdbcType="BIGINT"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="user_phone" property="userPhone" jdbcType="VARCHAR"/>
        <result column="scenic_spot_id" property="scenicSpotId" jdbcType="BIGINT"/>
        <result column="scenic_spot_name" property="scenicSpotName" jdbcType="VARCHAR"/>
        <result column="appointment_time" property="appointmentTime" jdbcType="TIMESTAMP"/>
        <result column="number_of_people" property="numberOfPeople" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="remark" property="remark" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 查询字段 -->
    <sql id="Appointment_Column_List">
        id, user_id, user_name, user_phone, scenic_spot_id, scenic_spot_name, appointment_time, 
        number_of_people, status, remark, create_time, update_time
    </sql>

    <!-- 管理员查询预约列表 -->
    <select id="selectForAdmin" resultMap="AppointmentResultMap">
        SELECT 
        <include refid="Appointment_Column_List"/>
        FROM appointment
        <where>
            <if test="userName != null and userName != ''">
                user_name LIKE CONCAT('%', #{userName}, '%')
            </if>
            <if test="scenicSpotName != null and scenicSpotName != ''">
                AND scenic_spot_name LIKE CONCAT('%', #{scenicSpotName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!-- 管理员查询预约总数 -->
    <select id="selectCountForAdmin" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM appointment
        <where>
            <if test="userName != null and userName != ''">
                user_name LIKE CONCAT('%', #{userName}, '%')
            </if>
            <if test="scenicSpotName != null and scenicSpotName != ''">
                AND scenic_spot_name LIKE CONCAT('%', #{scenicSpotName}, '%')
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>
</mapper>
